#include "inspirecv/core/image.h"

using namespace inspirecv;

int main() {
    // inspirecv::Image image = inspirecv::Image::Create();
    // image.Read("../images/r0.jpg");

    // inspirecv::Image image_copy =
    //   inspirecv::Image::Create(image.Width(), image.Height(), image.Channels(), image.Data());

    // inspirecv::Image image_no_copy = inspirecv::Image::Create(
    //   image.Width(), image.Height(), image.Channels(), image.Data(), false);

    // inspirecv::Rect2 rect = inspirecv::Rect2::Create(100, 100, 200, 200);
    // image.DrawRect(rect, {255, 0, 0}, 2);
    // image.Show("image");
    // image_copy.Show("image_copy");
    // image_no_copy.Show("image_no_copy");

    // image.Fill({100, 100, 200, 200}, {255, 0, 0});

    // image_no_copy.Show("image_no_copy_after_fill");

    return 0;
}
